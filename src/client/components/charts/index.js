import { timeseries as timeSeriesConfig, features as timeSeriesFeatures } from './timeseriesConfig'

export default {
  timeseries: {
    config: timeSeriesConfig,
    features: timeSeriesFeatures
  }
}